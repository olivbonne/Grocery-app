# Project notes for Claude

## Workflow / git

- **Commit and push all changes directly to the `main` branch.** The user has
  asked that future changes go straight to `main` rather than to a feature
  branch for later review/merge. Do not open a pull request unless explicitly
  asked. Always pull the latest `main` before editing so changes apply on top
  of the current state. This instruction stands until the user says otherwise.
