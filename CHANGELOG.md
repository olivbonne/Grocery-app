# Market List — Changelog

A running logbook of every change to `index.html`, newest first.

The version **number** shown at the bottom of the app (e.g. `v0.30`) corresponds to the
entries below. The number is paired in the app with the file's automatic
**last-modified date/time** (shown on hover / in the element's title), so each deployed
build is traceable to both a number here and the moment the file was last saved.

Convention: bump the number by `0.01` for each batch of changes, add a dated row here.

| Version | Date | Changes |
|--------|------------|---------|
| v0.87 | 2026-07-11 | **AI diagnostics + sandbox (hidden).** Long-press the version number at the bottom of Settings to open a read-only diagnostics panel: a live **entry tester** (type an item → see its category, meat group/subcategory, aisle rank at the current store, and AI rank), the **learned AI shop order per store** (accepted vs learning), and **restock cadence** per item. Plus a **🧪 sandbox list** button to test entry/sorting/predictions in a separate throwaway list without touching the real one. |
| v0.86 | 2026-07-11 | **Custom stores.** Settings → Sort → Store now has an **＋ Add store** button; custom stores are shared with the household (saved in the synced list) and can be renamed inline or removed. They have no aisle map, so they sort by category (like Coles Traders / Asian Richmond). |
| v0.85 | 2026-07-11 | **Buy-again no longer vanishes across devices.** The list is stored as one document with last-write-wins, so two people editing near-simultaneously could silently overwrite each other's "Buy again" additions (why mushroom/berries disappeared). Buy-again is now a conflict-free set: every entry carries a last-touched timestamp, deletions are recorded as tombstones (`baTomb`), and each incoming sync **merges** remote + local instead of clobbering — an item checked off on one phone can't wipe another's, and intentional deletes still stick. History cap raised **80 → 250**. |
| v0.84 | 2026-07-11 | **"unit" label on weightless items.** Items with no weight now show a muted **unit** tag where a weight/quantity would appear — on the shopping-page pills (in shop mode) and on both "Buy again" chip styles — so it's clear the quantity counts pieces, not grams. |
| v0.83 | 2026-07-11 | Consolidate singular/plural duplicates (e.g. "bbq chip" + "bbq chips") via plural-folded duplicate detection; move ✨ AI sort left of Location in Settings; suppress AI sort/learning when Home or "Other" store is selected (falls back to Location); add "Reset AI order for {store}" button. |
| v0.82 | 2026-07-06 | **A–Z sorts purely A–Z under Category display.** With **Display = Category** and **Sort = A–Z**, items inside each category now sort **strictly A–Z by name**, ignoring subcategory — previously the planning page re-grouped by subcategory first (so *milk, cheese* wouldn't interleave alphabetically). Shopping page already behaved this way; the two now match. |
| v0.81 | 2026-07-06 | **Toiletries → Health.** The categoriser now files **shampoo, conditioner, soap, toothpaste, toothbrush, floss** under **💊 Health** instead of Bulk (where they sat next to pantry staples like rice & flour). Cleaning/household items (detergent, tissue, toilet paper, paper towel, sponge, battery, foil) stay in Bulk. Consequence in **Location** sort: those toiletries now follow the existing "health pinned to the end of the walk" rule, so they group with vitamins/medicine at the bottom rather than mid-aisle. |
| v0.80 | 2026-07-06 | **Shop-page ✨, dismissible Smart-add hint, straight-up cart pop.** The **✨ Smart add** button now appears on the **shopping page** too (not just planning) — its preview sheet is wired on both pages via a shared `wireSmartSheet()`. The Smart-add hint line is now **dismissible** with an × (persists via `ml_hint_rechint`). The cart-landing pop no longer drifts right — it lifts **straight up** while doubling. |
| v0.79 | 2026-07-06 | **Removed the cart sparkle.** The green sparkle burst that fired when an item landed in the cart is gone; the cart still lifts up-and-right and doubles in size on arrival. |
| v0.78 | 2026-07-06 | **Shop-mode animation & counters (A23).** The bottom nav now shows **"N to buy"** and **"N in cart"** as two stats flanking the cart (divider stays between "to buy" and the cart); the checked-count badge is no longer stuck on the cart icon, and "in cart" always shows (even **0 in cart**). The **checked drawer** header reads **"N in cart"** with a green ✓ tick. Tapping an item in shop mode now **shows its tick, pauses briefly, then flies** a clone of the tile along a **rightward-bowing arc that's 30% slower**; on arrival the **cart lifts up-and-right while doubling in size (50% longer)** and throws a **green sparkle burst**. |
| v0.77 | 2026-07-06 | **UI batch (A22).** ① The **Smart-add editor** now lays each item's controls out top-to-bottom as **Quantity → Weight → Category → Subcategory** (subcategory chips appear when the chosen category has them). ② The **item edit sheet** row reads left-to-right **qty → unit/weight → name**, with a **CATEGORY** label above the category chips (matching the existing Subcategory label). ③ **Recipe import folded into ✨ Smart add** — the separate "Add from a recipe" button is gone; paste a whole recipe into the add box and Smart add detects it, routes to `/api/recipe`, and shows the servings scaler. ④ The **shopping-mode banner** and **"At the store?"** banner now use the same **cart glyph** as the bottom nav (no more 🛒 emoji). ⑤ The bottom-nav **"N left"** count now sits **right beside the cart**, separated by a thin divider, matching the design. ⑥ The **fly-to-cart animation** now launches a **live clone of the checked tile** (not a generic emoji) along a **smooth downward arc** (no upward hop). ⑦ The **cart icon doubles in size** (2×) when an item lands in it. |
| v0.76 | 2026-07-06 | **🔀 Merge duplicates (B5).** When several people add overlapping items — *milk, whole milk, 2% milk* — a **"Possible duplicates"** card in planning proposes combining them into one, with quantities summed. It's a **suggestion only** (a per-cluster **Merge** button + **×** to keep separate, and **Not now** to hide the batch), and every merge is **reversible via Undo**. Detection is fully **on-device** (no AI): items only cluster when one name refines another (e.g. *milk* ⊆ *whole milk*), so real variants combine while genuinely different products — *chicken breast* vs *turkey breast* — are left alone. |
| v0.75 | 2026-07-06 | **📖 Recipe → list (B3, text-first).** A new **"Add from a recipe"** link in planning opens a paste box; the AI pulls out just the ingredients and drops them into the familiar **Smart-add preview**. There you can **scale by servings** (a −/+ stepper recomputes quantities from the recipe's own yield), edit any item's category/qty/weight, and see a **"in list"** tag on anything you already have — so you don't blindly re-add. Nothing lands until you tap **Add**. Runs **server-side** on a new `/api/recipe` function (same Groq key, never in the browser); until `GROQ_API_KEY` is set it says so and typing still works. See `docs/ai-setup.md`. |
| v0.74 | 2026-07-06 | **🔁 Predictive restock (B4).** The app now learns how often you buy each thing (it quietly logs a purchase whenever you clear checked items) and, on a schedule you choose, shows a **"Time to restock?"** card in planning with the items that are due — e.g. *"Milk ~5d"*. It only ever **suggests** — nothing is added until you tap a chip; each suggestion has its own dismiss, plus **Not now** for the whole batch and a **Why?** explainer. New in **Settings › Predictions**: a global **On/Off**, **Weekly** (pick the day, Mon–Sun) or **Monthly** cadence. Fully **on-device** — purchase history never leaves your phone. Defaults to on, weekly on Monday. |
| v0.73 | 2026-07-06 | **✨ AI shop order — propose, don't reshuffle (B7).** The learned aisle order is now a **suggestion you accept**, not a silent takeover. The list follows the order you've **accepted**; the AI keeps learning quietly in the background, and when it has a better route it offers a card at the **start of your next trip** — *"New aisle order — Apply / Keep current."* Once you start checking things off, the order is **frozen for that trip** (no disruptive mid-shop reshuffle), and learning only affects the *next* proposal. Fully on-device; per store. |
| v0.72 | 2026-07-06 | **✨ Smart add — editable preview (B1).** The AI preview chips are now fully editable before anything is committed: tap any parsed item to open an inline editor and change its **category** (tap a category chip), **quantity** (− / + stepper), or **weight** (100g…5kg, or unit). Nothing lands on the list until you tap **Add**, and the edits carry through — so a wrong guess is a one-tap fix, not a delete-and-retype. Removing a chip and the not-configured/offline fallbacks are unchanged. |
| v0.71 | 2026-07-05 | **Shopping mode polish + Settings.** Tapping an item in shop mode now flies a miniature of it along a curved arc into the **Cart** tab, which bounces as it lands; rapid taps queue ~80ms apart. Progress is mirrored in three places at once — the item list, the redesigned **Shopping mode** banner (cart badge · **N to buy** · **N in cart**), and the bottom **Cart** tab (with a middle **N to buy** stat) — so you always see it change. Every shop-mode item shows a **check circle** (○ → filled ✓ when in the cart). "Options" is renamed **Settings**; the shop **Sort** option "Category" is now **Subcategory**; a new **Users** row lists everyone's full name (tap to switch who you are) and the single-letter avatars were removed from the header. Wording: "left to get"/"left" → **to buy**. Respects reduced-motion. |
| v0.70 | 2026-07-05 | **✨ AI shop order.** Shopping mode gets a new **✨ AI** sort (Options → Flat list → Sort, shop mode only). It orders your list by the route you actually walk: it starts identical to **Location** (store layout), then quietly learns the order you check categories off at each store and reorders your next trip to match. Fully **on-device** — no AI call, nothing leaves your phone; the learned order is kept per store so a big-box run and the corner shop can differ. A small note shows whether it's still using the store layout or has learned your route. |
| v0.69 | 2026-07-05 | **✨ Smart add (AI).** In planning mode there's a new **✨** button next to the add box. Type anything casual — "2 milk, stuff for tacos" — and it asks an AI to turn it into real grocery items, then shows them as a **preview** (each tinted with its category, e.g. Milk → Fresh, Tortillas → Bulk) that you can trim before anything is added. Nothing lands on your list until you tap **Add**. The AI runs **server-side** (a new `/api/parse` function on Groq / llama-3.1-8b-instant) so the API key never touches the browser; until a `GROQ_API_KEY` is configured in Vercel, Smart add says so and plain typing keeps working. See `docs/ai-setup.md`. |
| v0.68 | 2026-07-05 | **Cart tab + Amazon-style badge.** The bottom tab is now **Cart**. It shows the **planned** (unchecked) count to the left of the cart, and once you start checking items off a red **checked** count appears on the cart icon (like Amazon) — the numbers pulse when they change. **Planning and shopping now keep separate settings:** text size, style, display and sort are remembered independently for each mode, so a big-text shopping view doesn't change your planning view. **Removed the microphone button** next to the add box (your keyboard already has dictation). |
| v0.67 | 2026-07-05 | **One list page (Option B).** The separate "Choose" and "Shop" tabs are gone — there's now a single **List** page and the bottom bar is just **Lists · List**. It opens in planning mode (solid items are on the list, faded ones are past buys you tap to re-add); tap **🛒 At the store? Start shopping** to switch the same page into shopping mode (big tap targets, faded items hidden, check things off, **Clear → Buy again**), and **Done** to switch back. No more hunting for a hidden planning page — planner and shopper use the same surface. The List tab shows the item count. |
| v0.66 | 2026-07-04 | **"Plan" is now "Choose."** The middle tab was invisible to planners (a "+" icon read as "add", not "plan the shop"). It's renamed **Choose** with a checklist icon, joining a list now lands there first, and a one-time dismissible hint explains it: solid items are on the list, faded ones were bought before — tap to add or remove. Opening a list from the Lists page also starts on Choose. |
| v0.65 | 2026-07-04 | **🏠 Home is now a real place:** under Sort=Location you can pick Home *and* save its GPS location ("📍 Set 🏠 Home location here"), so Auto-detect recognises when you're at home (sorts by category) vs at a saved store (aisle order). **Swipe to switch pages:** swipe left/right anywhere to move between Lists ⇄ Plan ⇄ Shop. **Buy again keeps its memory:** re-*typing* a cleared item now returns it to its remembered category *and* subcategory (tapping it already did). **Simpler counters:** the Lists page badge shows just the total items; the checked-item count next to the Shop icon is gone (checked items are temporary). **Shopping mode is back:** it had become unreachable — a "🛒 At the store? Start shopping" bar on the Shop page now turns it on (bigger tap targets, editing hidden), Done turns it off. Plus small code cleanups. |
| v0.64 | 2026-07-04 | Design-audit polish pass (no layout changes). **Typography now works offline:** the Outfit font is self-hosted in the repo (2 small woff2 files, ~46KB) instead of loaded from Google Fonts — the brand type now renders on flaky store wifi and offline as a home-screen app, and the page makes one less third-party request. **Readability:** the light-mode muted text tone (counts, hints, empty states) darkened from 2.3:1 to 4.4:1 contrast (WCAG AA); checked-item text slightly darkened too. Dark mode already passed and is unchanged. **Easier tapping:** the small controls (category +, quick-edit ▾, clear ×, header icons) now have invisible ~44px hit areas — same look, harder to miss mid-shop. |
| v0.63 | 2026-07-02 | **Store picker:** added **🏠 Home** and **Other** — both sort by category (no GPS/aisle); Home for planning at home, Other for a store with no aisle map yet. The picker is now a single horizontal-scrolling row, and the **Options sheet now scrolls** (max-height) so nothing gets cut off when Location is on. **Plan · Category:** list items and dotted “Buy again” items now sort **together** by subcategory → A–Z, so tapping one to add/remove **keeps it in the same spot** instead of jumping. **Categoriser:** now reuses an item’s **existing** category (from the list or Buy again) before guessing, and when it does guess it picks the **most specific** keyword (longest match) — so “Popcorn butter” → bulk (not vegetable via “corn”), and re-typed items stop landing in the wrong category. |
| v0.62 | 2026-06-29 | Firebase config for project grocery-list-88109 is now baked into index.html — no more pasting it in on every deploy. (Firebase web config values aren't secrets; they're shipped to the browser by design.) |
| v0.61 | 2026-06-29 | Typing now suggests matching “Buy again” items first, shown as the full item with its quantity & weight (e.g. “↩ 1kg Chicken feet”, “↩ 2× 500g Chicken”) — tap to re-add it exactly. Options: Display (Category / Flat list) moved above Sort and is always available; when Display = Category, Location sort is no longer offered (you can’t walk aisles and group by category at once). Under Category display, A–Z now sorts items plainly within each category. Sound moved to just above Switch user. |
| v0.60 | 2026-06-29 | Checked items now sort newest-first (last item checked jumps to the top of the checked bar). Added two stores: Asian Richmond and Asian Glenferrie (aisle maps to come). New Display option (Category / Flat list) shown when Sort = Category — Flat list recreates the old Auto view (one flat list by category → subcategory → A–Z), now on both Shop and Plan pages. Sort control (Location · Category · A–Z) now sits on a single line as an equal-width segment. |
| v0.59 | 2026-06-29 | Sort options: removed Auto; now Location · Category · A–Z (left to right). Stores renamed (Coles Dandenong, Coles Vic Garden) and two added: Coles Traders (aisle map to come) and Dandenong market (shows only meat & seafood, vegetable, fruit and fresh — in that order, by category → subcategory → A–Z). Store picker in Options is now a full-width list. Checked items + the green “Clear N → Buy again” banner moved into a fixed bottom bar above the nav; it scrolls internally when there are lots of checked items. |
| v0.58 | 2026-06-29 | Reverted the flex app-shell (top/bottom banners back to the previous fixed approach). Checked items now sit at the bottom of the Shop list, with a green “Clear N checked → Buy again” banner as the very last row (moved out of the header). Added a second store: Coles Victoria Gardens Richmond (10-aisle map from photos), alongside Coles Dandenong. New Store picker in Options under Location sort: Auto / Dandenong / Richmond. Auto-detect uses GPS to pick the nearest known store; Options now shows which store you're in (or “Others”). “Set store here” records the selected store's real coordinates on-site. |
| v0.57 | 2026-06-29 | Top & bottom banners now stay fixed on every page via a flex app-shell (replaces fragile position:fixed). Location sort: meat & seafood pinned to the very top (category → subcategory → A–Z); health, Asian, alcohol pinned to the bottom in that order. Geolocation gating: when Location sort is on, the store aisle order shows only when you're at the saved store — otherwise it falls back to Auto. New Options row (shown with Location sort): On/Off toggle + “Set store here”. Off = aisle order always (no GPS used). |
| v0.56 | 2026-06-25 | Location sort now covers the full Coles Dandenong layout (aisles 1–14). Added the previously-missing aisles: 3 (drinks/water), 11 (shampoo/cosmetics/personal care), 12 (pet), 13 (toilet paper/tissues/frozen veg/fish). Perimeter dairy is placed per the store: eggs at the front with produce, butter by aisle 4, yogurt & cream by aisle 6, milk & cheese by aisle 13. |
| v0.55 | 2026-06-25 | Removed the leading **+** from not-yet-added (dotted) items on Plan. Options: **Appearance** renamed to **Mode** (Light/Dark); new **Appearance** toggle with **Full** (fills each bubble with its category colour on Plan and Shop) / **Empty** (current card look). New **Location** sort on the Shop (and Plan) page that orders items by Coles Dandenong aisle layout, with vegetables and fruit always on top. |
| v0.54 | 2026-06-25 | Plan bubbles no longer shift width when toggled. Selected and dotted items now reserve the same leading **+** slot and trailing **▾** slot (one is shown, the other is an invisible placeholder), so a given item is the exact same width whether it's in the list or in the dotted buy-again row. |
| v0.53 | 2026-06-25 | Typography: switched the whole app to **Outfit** (both body and headers), replacing Inter + Bricolage Grotesque. Strengthened link-preview meta: added **canonical**, **og:url**, **og:image type/width/height**, and **twitter:card=summary** so shared `?list=` links render reliably across platforms. |
| v0.52 | 2026-06-25 | Design polish pass (redesign audit). Fixed the **theme-color** meta tag (was an invalid CSS var; now real hex and follows the manual dark toggle). Switched `100vh` → **`100dvh`** for correct iOS Safari height. Added **tabular figures** to all counts/quantities so numbers don't shift width. Added **press feedback** (scale on tap) and 150ms transitions to pills, bubbles, and nav. Added **focus-visible** outlines for keyboard/AT users. Removed the vestigial **blue second accent** (buy-again header now neutral). **Tinted the header shadow** toward the ink green. Added **description + Open Graph** tags so shared `?list=` links preview properly. |
| v0.51 | 2026-06-25 | Item names now display with a **capital first letter** by default. **Plan page sounds**: tapping a dotted item to add it plays a rising tone; tapping a selected item to send it to buy-again plays a falling tone (when Sound is on). The **dotted (buy-again) bubbles** on Plan are now the **same size** as selected items. **Plan · Auto** no longer inserts a line break between subcategories (continuous wrap within each category). |
| v0.50 | 2026-06-25 | **Plan · Auto** now keeps the **category header chips** (with collapse and per-category +). Within each category, selected and dotted items are combined and sorted by subcategory → A–Z, with a line break after each subcategory. |
| v0.49 | 2026-06-25 | Sort logic refined. **Plan · Category**: selected items now sort by subcategory then A–Z within each category; unselecting an item now sends it to the **end** of the dotted (buy-again) list. **Shop · Auto**: now a flat list ordered by category → subcategory → A–Z. **Plan · Auto**: now sorts selected *and* dotted items together by category → subcategory → A–Z, with a line break after each subcategory group. |
| v0.48 | 2026-06-25 | App is now **locked to portrait** (manifest orientation + a rotate-to-portrait overlay in landscape). The **Shop** page is now purely for shopping: **Buy again** (all in Plan now), the **+ New category** button, and the **progress bar** were removed. Bottom-bar counts changed: the **Plan** tab shows the **total** item count and the **Shop** tab shows only the **checked** count. |
| v0.47 | 2026-06-25 | List **reordering** moved into the long-press action sheet (**↑ Move up / ↓ Move down**) and removed from the Options menu. On the **Plan** page, the **Auto** sort now groups items by category (like the Shop page's Category view) while keeping insertion order within each group; **A–Z** stays a flat list. |
| v0.46 | 2026-06-25 | **Sort** is now per-page and gains an **Auto** option (Auto · Category · A–Z) — Auto keeps items in the order added (newest last, above the not-yet-added items on Plan). Selected items on the **Plan** page now use the same pill style as the **Shop** page. New lists are now named via an **in-app input** (cursor + keyboard, no default name) instead of a browser prompt. The Options menu on the **Lists** page can now **reorder lists** (↑↓), and each list bubble shows a **›** chevron. Removed the **›** from the Plan header; the Plan top bar now matches the Shop top bar exactly (and reserves the status-bar space). |
| v0.45 | 2026-06-25 | Navigation is now a chain: **Lists → (tap a list) → Plan → (›) → Shop → (‹) → Plan → (‹) → Lists**. The Plan header shows **‹ list-name ›** and now also shows collaborators, with the input bar positioned exactly like the Shop page so switching doesn't jump. On **Plan** you can add items per category with **+**, each item has the quick **▾** (qty/weight) control, and the dotted buy-again items' edit dialog now says **Buy again** (with "Add to list"). The Shop page's per-item **▾** was removed. **Lists** bubbles show a `done/total` pill and a progress bar. New per-page settings: **Zoom** and a **Style** (Compact / Normal) can now be set independently for the Plan and Shop pages. |
| v0.44 | 2026-06-25 | The **Plan** button (now also the middle button on the Lists page) opens the Plan page instead of creating a list (use the **+ New list** bubble for that). On the **Plan** page you can now **long-press** any category or item — in-list or not — to edit it, **tap a category to collapse/expand** it, and there's a **+ New category** button. Removed the explainer text above the first Plan category. **Collapse/Expand all** in Options now acts on whichever page you're on (Plan vs Shop) independently. The **Shop** input placeholder now reads "Add items to *[list]*…", matching the Plan page. |
| v0.43 | 2026-06-25 | **Checked items** section now sits at the **top** of the list. Cart icon stays **centred over its label** with the `0/16` count floated to its right. Bottom-nav labels renamed: middle button is now **Plan** and the cart is now **Shop**. The **Plan** page now shows the **list's name** in the header (not "Plan") and displays items **already on the list with a filled background** vs. previous/buy-again items as **dashed outlines** — tap a filled one to remove it, tap an outlined `+` to add it. |
| v0.42 | 2026-06-25 | **Checked items** now collect into their own "✓ Checked items" section at the bottom instead of greying out inside each category (tap one to send it back to its category; category counts now show only what's left to buy). **Cart** tab now opens the **currently selected list** (the separate "shopping mode" view is retired), and **+ Add** now opens a **Plan** page showing all your *buy again* items to tap-to-add, plus the add bar. **Lists page** bubbles show a **progress bar** and **done/total items** (e.g. `1/6 items`). The active-tab indicator bar now sits on the **top edge** of the bottom nav. The cart count (`0/16`) moved to the **right of the cart icon**. Removed the "to buy" count from the list header; the header's top-right now reads **users · synced · ⋯ options** (right-to-left: options, synced, users). |
| v0.41 | 2026-06-25 | Back arrow on a list now shows just **‹** (no "Lists" label). Removed the list-name row beneath the input; **Collapse/Expand all** moved into **Options** as the first item. In the Lists page action sheet, "Copy link" is replaced by **Duplicate list** (creates a fresh copy named "… copy", items reset to unchecked). The **Cart** badge now reads **done / total** (e.g. `0/16`, ticking to `1/16` …) instead of just the remaining count. |
| v0.40 | 2026-06-22 | Reworked the bottom nav like a shopping app: **Lists · + Add · Cart** (left→right). The **Cart** shows a live badge of how many items are still to buy and opens shopping mode; the active tab gets a coloured indicator bar above it. **Options** moved to a button in the top-right of the fixed header. On the **Lists page** the bottom nav is **Lists · + Add · Cart**, where + Add creates a new list and Cart opens the current list's shopping mode. Added the dedicated **Lists page** (large bubbles with per-list counts; tap to open, press-and-hold to Rename/Share/Copy/Delete with Undo) and a **‹ Lists** back arrow on the detail view, which is titled with the list's name. Instant in-app list switching (theme no longer resets). Pinned Clear/Shopping banners, full-width category chips, collapse-all includes Buy again, non-wrapping Undo button, centered zoom %. |
| v0.39 | 2026-06-22 | **Buy again is now a fully editable category:** long-press its chip to change the emoji/picture, rename it, reposition it among the other categories (↑↓), delete history, and save (stored in `baMeta`). Buy-again items now display plainly (qty · weight · name) like any list item; the **subcategory is retained** when an item is swept here (previously lost) and editing a saved item shows the **same full editor** as a normal item (category + subcategory chips). New lists now show the name you gave them immediately (falls back to the locally-stored name until the doc syncs). New/empty lists start with **no visible categories** — categories appear automatically as items are added (empty default categories are hidden; user-created categories stay visible). |
| v0.38 | 2026-06-22 | **Layout overhaul (per request):** add bar moved back to the **top** inside a **sticky** region (header + progress + input + suggestions + control row stay visible while the list scrolls), which also fixes the post-typing scroll lock. New **permanent bottom bar** with **Lists** and **Options (⋯)**. The **list name** sits on the control row next to Collapse/Expand-all. **Options** menu gathers appearance (light/dark), text-size zoom −/+, sound on/off, sort **by category / A–Z**, switch user, share link, and the version number. **Lists** menu supports multiple lists — create, rename, and switch (stored locally + list name synced in the doc). **Buy again** is now a category-style section with a blue ↩ chip and item count, and each saved item shows its **subcategory**. Removed the per-item "who added" badge (backend handles identity; adds still de-dupe). Quick-edit panel now closes when you tap outside it. "none" weight relabelled **"unit"**. |
| v0.37 | 2026-06-22 | **Stage 1 (in-store + accessibility):** A3 Shopping mode (cart toggle — bigger glanceable rows, dims checked, hides editing chrome, sticky banner with Done); A7 per-item attribution (records `checkedBy`, shows a small adder/checker initial when >1 member; presence avatars already existed); A10 respect system `prefers-color-scheme` as the default theme; A12 friendly empty state with example chips + share hint; A14 honour `prefers-reduced-motion`; A15 native share sheet (`navigator.share`) with clipboard fallback; **A13 inline quick-edit** (tap the ▾ on a row → − N + stepper and a scrollable weight cycle, no full sheet; long-press still opens the full editor). A4 aisle order already shipped (reorder via edit-category ↑↓). **Fixes:** iOS keyboard no longer hides the bottom add bar (VisualViewport lifts it above the keyboard, only while an input is focused so page scrolling doesn't jitter it); undo toast no longer slides sideways on appear (centering-preserving animation); confirmed A1 dynamic bottom padding. Also includes the recent colour-palette picker, filled download arrow, 6-cell icon window, auto-best icon while typing, buy-again weight retention, weight "none" label, top-pinned collapse-all, and no white strip on zoom-out. |
| v0.36 | 2026-06-21 | Zoom reworked with `transform: scale()` + `transform-origin:top left` + width compensation (`width: 100%/density`) and a JS height-sync, so side margins are equal at every zoom level and the page reserves the correct height (predictable on Safari, unlike CSS `zoom`). |
| v0.35 | 2026-06-21 | Fixed the colour square rendering (a later `.picopt` rule was overriding its background, so it showed as a dark box); it now shows the category colour in a rainbow ring. Added separation + scroll-snap so a 7th+ emoji can't clash into it. Reverted the earlier zoom width-compensation attempt that broke `zoom`. |
| v0.34 | 2026-06-21 | Deleting a category now moves its items (and buy-again entries) to **Others**. Replaced the 😀 emoji-keyboard button with a category-colour **colour-change square** (opens the colour picker); kept ⬇ upload. |
| v0.33 | 2026-06-20 | First attempt to equalise zoom-out side margins via width compensation on the `zoom` wrap (later reverted in v0.35 for breaking zoom). |
| v0.32 | 2026-06-20 | Edit-category icon row: left group became a horizontally scrollable window through every keyword-relevant picture/emoji (current selection first); 😀 and ⬇ fixed on the right. |
| v0.31 | 2026-06-20 | Edit-category icon row set to a fixed 8 cells that fit without scrolling (current + 5 relevant, then 😀 and ⬇). Greatly expanded the relevant-emoji library (~140 entries). |
| v0.30 | 2026-06-20 | Edit-item: name + weight + quantity now on one line at equal height. Version footer now shows a version **number** (linked to the auto last-modified date via tooltip). Added this changelog. Edit-category icon picker reworked: relevant pictures/emoji shown in one always-visible row filtered strictly by the category name keyword, current selection pinned to the right, 😀 (position 7) opens the native emoji keyboard for full keyword choice, ⬇ (position 8) uploads from photos/files. |
| v0.29 | 2026-06-20 | Version number tied to automatic file last-modified date. Fixed oversized category clear ✕ (a `.catadd button` CSS rule was resizing it). Moved Collapse/Expand-all onto the first category's row, right-aligned. Category emoji updates live while typing the name. Confirmed sound intact. Added weight rolling selector (blank/100g–900g/1–3kg) in edit-item. |
| v0.28 | 2026-06-19 | Version footer (manual tally) added. Clear ✕ centred vertically in all input boxes. Edit-category emoji ordered by category name with category-colour backgrounds. Added star toggle in edit-item (gold ★, sorts starred to top). |
| v0.27 | 2026-06-19 | Category clear ✕ unified with main; padding so text clears the ✕. Collapse-all aligned to first category. Bulk pills use the purple category line in both themes. Asian bowl shows white in dark theme inside the edit-category sheet. Edit-category icon background uses the category colour (icon box + first cells). Removed “— commas for several” from category placeholders. |
| v0.26 | 2026-06-19 | Icon picker cells share width to fit 8 without scrolling; larger emoji. “Add subcategory” now creates an inline editable row (no popup). Asian bowl regenerated pure white for dark theme. |
| v0.25 | 2026-06-18 | Distinct check vs uncheck sounds. Clear ✕ moved to top-right, consistent across inputs, text padded. Reworked edit-category icon picker (one line, max 8: keyword pictures+emoji, 😀 grid, ⬇ upload), “x of y” above arrows, subcategories on top, smaller editable rows, “Remove”→“Delete”. Reverted the broken full-width-zoom experiment. Health cross given white interior inside the green ring. |
| v0.24 | 2026-06-18 | Collapse/Expand-all control. Dark mode set as default. Firebase config moved to a clearly-marked block at the very top of the file. One-line edit-category header (icon + name + up/down) with position indicator. Subcategory rows with up/down reordering. Picture shown in the icon box for default-picture categories with a long-press picker. Main-list category headings restyled as coloured chips matching the edit-item chips. Input clear (✕) buttons + draft persistence so typing isn’t lost on re-render. Bulk grey-surround treatment (later revised). |
| v0.23 | 2026-06-18 | Star items (gold ★, sort to top). Weight selector in edit-item. |
| v0.22 | 2026-06-17 | Firebase config relocated to top of file; module reads `window.FIREBASE_CONFIG`. Auto version footer (date-based) introduced. App Store / Siri / monetisation guidance delivered (not a code change). |
| v0.21 | 2026-06-17 | New-category sheet with icon picker; centred SVG “+”; doubled “+” size. Mic moved to the left of the input; orange while recording. Input clear ✕ (centred, consistent) and draft persistence first added. |
| v0.20 | 2026-06-17 | Emoji + picture category icons (milk / cross / ramen defaults, hammer preset, dark variants). Dark-mode image variants. PIL image processing: transparency via border-flood, interior-white fill, recolouring, grid splitting, largest-component keep (drop the hammer’s shadow). |
| v0.19 | 2026-06-16 | Create / remove categories and subcategories; inline editable subcategory rows with reordering. |
| v0.18 | 2026-06-16 | Meat / general subcategory grouping; checked items drop to the bottom of their category. |
| v0.17 | 2026-06-16 | Quantity per item (shown “N×”). A–Z sort within categories. |
| v0.16 | 2026-06-16 | Collapse / expand categories and the Buy-again section. |
| v0.15 | 2026-06-15 | Long-press to edit items, categories, and buy-again entries. |
| v0.14 | 2026-06-15 | Pop animation + tick sound on check; sound on/off toggle (off by default). |
| v0.13 | 2026-06-15 | Progress tally + bar in the header. |
| v0.12 | 2026-06-15 | Typing autocomplete suggestions (from buy-again, items, dictionary). |
| v0.11 | 2026-06-14 | Mic + voice capture (Web Speech API; unreliable on iOS Safari — noted). |
| v0.10 | 2026-06-14 | Compact density zoom (+ / −). |
| v0.09 | 2026-06-14 | Smart labelled-paste: multi-line / “Category:” blocks parsed into the right sections. |
| v0.08 | 2026-06-13 | Per-category “+” add boxes; always-visible categories. |
| v0.07 | 2026-06-13 | Buy-again history: cleared items land in a re-add section. |
| v0.06 | 2026-06-13 | Keyword auto-categorisation dictionary. |
| v0.05 | 2026-06-12 | Item pills with category colour accents; tap to check. |
| v0.04 | 2026-06-12 | Default category set (meat, fruit, vegetable, fresh, bulk, health, asian, alcohol, others) with colours. |
| v0.03 | 2026-06-12 | Firebase Firestore live sync across household members (up to 4). |
| v0.02 | 2026-06-11 | Shareable list via `?list=` code; basic add/remove. |
| v0.01 | 2026-06-11 | Initial single-file PWA scaffold (HTML/CSS/JS), manifest, app icons. |

> Dates before v0.22 are approximate (reconstructed from the build history); from v0.22 onward
> the app’s footer also carries the exact file last-modified timestamp for each deployed build.
